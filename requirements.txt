# 企微数据中转 MCP Gateway 运行依赖
# Python 3.11+

# MCP 官方 Python SDK（提供 FastMCP 与 Streamable HTTP transport）
mcp>=1.2.0

# Web 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.9          # 管理后台上传可信域名校验文件

# 企微 API 调用
# The declarative SSRF boundary installs an httpcore network backend before a
# connection pool is created; keep that integration on the tested httpx line.
httpx==0.28.1
httpcore==1.0.9

# 数据库
sqlalchemy>=2.0.0
pymysql>=1.1.0
cryptography>=43.0.0          # pymysql caching_sha2 需

# 配置与校验
pydantic>=2.9.0
pydantic-settings>=2.5.0
python-dotenv>=1.0.0
PyYAML>=6.0
argon2-cffi>=23.1.0

# 调度（一期定时轮询）
apscheduler>=3.10.0

# 测试
pytest>=8.0.0
pytest-asyncio>=0.24.0
