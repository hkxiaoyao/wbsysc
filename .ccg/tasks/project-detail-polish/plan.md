# Implementation plan index

The executable implementation plan is stored at:

`docs/superpowers/plans/2026-07-13-project-detail-polish.md`

The plan contains seven independently testable tasks:

1. Restore the test baseline and enforce production configuration
2. Persist tenant data mode and propagate runtime context
3. Add the MySQL and WeCom data-access boundary
4. Delegate MCP tools to the data-access boundary
5. Make stored synchronization complete and queryable
6. Protect tokens and expose data mode in the admin UI
7. Align SQL and documentation, then run final verification and review
