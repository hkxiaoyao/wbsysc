# 需求

在连接实例的 Token 管理区域提供持续可见的“一键生成并复制 MCP 配置”入口。

## 验收标准

- 点击后签发新的连接 Token，不轮换、不撤销已有 Token。
- 复制内容包含实例专属 `/mcp/{connection_id}` URL 和 Bearer Token。
- Token 原文继续只在本次响应及一次性弹窗中存在，关闭弹窗后清除。
- 剪贴板失败时仍显示完整配置，允许用户手动复制。
- 管理员与租户连接实例页面共用同一实现。
- 补充前端单元测试并通过构建。
