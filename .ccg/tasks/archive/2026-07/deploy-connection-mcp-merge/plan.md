# 发布计划与执行记录

1. 核对本地 `main`、`origin/main` 和生产服务器工作区，只保留既有未跟踪备份文件。
2. 推送 `f58b879` 并等待 GitHub Actions 镜像构建成功。
3. 只读盘点旧 MCP 服务、连接绑定和旧服务令牌，跨连接数据作为阻断条件。
4. 备份中心数据库和 `.env`，将旧 MCP 兼容入口明确设为关闭。
5. 执行 004–010 数据库迁移并部署新镜像。
6. 验证容器镜像、健康状态、管理后台页面和近期错误日志。
7. 完成发布复核，归档任务并推送发布记录。

# 实际执行差异

- GHCR 拉取在一个镜像层持续卡住；部署脚本启动旧镜像后，新版健康检查因缺少新字段正确判定失败。
- 改为基于生产服务器已检出的 `f58b879` 源码本机构建。构建期间仅临时使用腾讯 PyPI 镜像加速依赖下载，未修改仓库文件。
- 最终容器镜像 ID 与 `wbsysc:f58b879` 镜像 ID一致。
