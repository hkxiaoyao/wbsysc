# 发布目标

- 将本地 `main` 的 MCP 融合改造推送到 `origin/main`。
- 部署到 `211.159.172.117:/root/app/wbsysc`。
- 发布前只读盘点存量 MCP 服务，跨连接服务不得静默拆分或删除。
- 使用项目既有部署流程，不跳过数据库迁移和健康检查。
- 发布后验证容器状态、`/health`、连接级 MCP 路由和管理面服务路由收口。
- 不执行安全扫描或联网安全测试。
