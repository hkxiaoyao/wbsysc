# 需求

- 恢复企业微信可信域名校验文件的查询、上传、公开访问和删除能力。
- 校验文件归属从租户迁移为企业微信连接实例；同一租户的多个连接互不覆盖。
- 管理员与租户登录后台均可在连接工作台操作，并沿用各自现有权限边界。
- 可信域名继续以连接 `public_config.trusted_domain` 为唯一配置来源。
- 旧 `domain_verify_file.tenant_id` 数据不得丢失；历史企业微信连接可自动认领旧文件。
- 根路径公开文件 URL 继续兼容企业微信验证流程。
- 上传限制保持为安全文件名、UTF-8 文本且不超过 64 KiB。

