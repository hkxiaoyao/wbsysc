# 实施计划

1. 为 `domain_verify_file` 增加可空 `connection_id` 和连接级唯一索引，移除租户级唯一约束，保留 `tenant_id` 作为归属和兼容字段。
2. 扩展校验文件存储函数，支持按连接查询、保存、删除，并允许确定性的历史企业微信连接认领旧租户文件。
3. 在连接管理用例中增加查询、上传、删除操作；管理员与租户路由复用同一所有权校验。
4. 在企业微信连接工作台增加校验文件选择、上传、当前文件 URL、复制与删除界面。
5. 增加迁移、后端权限/API、存储行为及前端端点/UI 测试。
6. 运行目标测试、全量测试和生产构建，完成普通代码审查后归档、提交、推送并部署。

