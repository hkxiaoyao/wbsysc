# 需求

- HTTPS 代理访问时，应用必须正确识别原始 HTTPS scheme，使同源保护写操作可用。
- 生产应用端口 8001 只绑定宿主机回环地址，禁止绕过 Nginx 直接访问。
- Uvicorn 只信任 Docker 私网代理来源，不使用不受限的 `*`。
- 保留严格同源校验、生产 `Secure` Cookie 和现有认证行为。
- 增加部署配置回归测试，运行全量测试、前端构建并部署验证。
- 不执行安全测试。
