# 需求

## 目标

连接实例的 Token 创建后，用户仍可在 Token 列表中随时一键复制该 Token 对应的 MCP 配置，不再依赖创建成功时的一次性弹窗。

## 验收标准

- Token 列表为可用 Token 提供明确的“复制 MCP 配置”操作。
- 复制内容包含对应连接实例的 MCP 地址和可用 Bearer Token。
- 创建 Token 后仍保留现有一键复制体验。
- 撤销的 Token 不允许复制可用配置。
- 管理后台和租户后台的权限边界保持不变。
- 后端与前端测试覆盖新增流程。
